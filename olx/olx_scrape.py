from bs4 import BeautifulSoup


def olx_scrape():
	print("Olx scrape")
    # with open('olx/olx_auti.htm', 'r') as f:
    #     contents = f.read()

    # soup = BeautifulSoup(contents, 'html.parser')

    # # Extract data
    # # This depends on the structure of the webpage. Here's an example of getting all paragraph texts.
    # paragraphs = soup.find_all('p')
    # for p in paragraphs:
    #     print(p.get_text())
    